spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rts_test?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=utf8
    username: root
    password: your_password

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
